
// This file exports all components from the UI directory to simplify imports
import { Alert, AlertTitle, AlertDescription } from "./alert";
import { Separator } from "./separator";

export { 
  Alert, 
  AlertTitle, 
  AlertDescription,
  Separator 
};
